# Optional only: used by explicit legacy compatibility commands and
# `flight research --provider seats.aero --fetch`.
# The default manual_import research workflow needs no provider key, account, or SQLite database.
SEATS_AERO_API_KEY=

# Optional local SQLite location. Leave unset to use data/flights.sqlite3.
# FLIGHTS_DB_PATH=
